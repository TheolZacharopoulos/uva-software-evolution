module CloneDetection::Utils::TreeHasher

import IO;

alias Hash = str;

Hash perfectHashTree(node tree) {
    Hash hash = "";
    // TODO;    
    return hash;
}

Hash badHashTree(node tree) {
    Hash hash = "";
    // TODO;    
    return hash;
}