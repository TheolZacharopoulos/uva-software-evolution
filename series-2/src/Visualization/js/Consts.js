const DATA_FILE = "demo_project/readme-flare-imports.json";
const DIAMETER = 800;
const DEGREES = 360;
const TENSION = .85;

// size of the wheel
const diameter = DIAMETER,
      radius = diameter / 2,
      innerRadius = radius - 120;
